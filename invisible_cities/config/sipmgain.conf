#!/usr/bin/python
# sipmgain.conf

# sipmgain is a CalibratedCity.
# It computes and writes binned spectra of zeroed waveforms.

include('$ICDIR/config/calibrated_city.conf')

# run number
run_number = 4821

# Processing type (temp, probably)
proc_mode = 'gain'

# Histogram bins
min_bin = -49.5
max_bin = 200.5
bin_wid = 1.

# integral definitions. Start, width and period in microseconds
number_integrals = 1
start_integral = 41
width_integral = 2
period_integrals = 50
